[pytest]
pythonpath = src
testpaths = tests
# Désactive le filtrage par défaut des marqueurs pour tout exécuter par défaut
addopts = -m ""
env_files =
    .env

# Déclaration des marqueurs pour éviter les warnings
markers =
    integration: tests contacting external APIs (Microsoft Graph, Vercel AI Gateway)
    unit: isolated unit tests